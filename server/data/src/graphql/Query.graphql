type Query {
    packageSets: [ContentPackageSet] # all packages grouped as sets
    packageSet(id: Int!): ContentPackageSet # single package set
    contentCategories: [ContentCategory] # all categories
    contentCategory(id: Int!): ContentCategory # single category
}