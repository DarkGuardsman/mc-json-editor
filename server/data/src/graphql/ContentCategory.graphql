# Type of content used for grouping
type ContentCategory @key(fields: "id") {
    id: Int! # database id
    name: String!# display name
    detection: ContentCategoryDetection # how to detect content type
}