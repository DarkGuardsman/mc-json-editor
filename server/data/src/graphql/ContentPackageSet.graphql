# Grouping of packages (buildcraft, mekanism)
type ContentPackageSet @key(fields: "id") {
    id: Int! # database id
    name: String! # display name
    packages: [ContentPackage] # packages to group
}