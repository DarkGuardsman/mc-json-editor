query GatherContentCategories {
    contentCategories {
        id
        name
        folderPrefix
        detection {
            mode
            alg
            fields {
                id
                regex
            }
        }
    }
}
