query ProjectFilesList($categoryId: Int!, $projectId: Int!) {
    project(id: $projectId) {
        content(id: $categoryId) {
            entries {
                name
                key
            }
        }
    }
}